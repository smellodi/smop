<Window x:Class="Smop.MainApp.Dialogs.DmsSaveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Smop.MainApp.Dialogs"
        xmlns:conv="clr-namespace:Smop.MainApp.Utils"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        Title="Save DMS data to Google Drive"
        Height="350"
        Width="570"
        WindowStyle="ToolWindow"
        Icon="/Assets/icons/main.png"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Unloaded="Window_Unloaded">
    <Window.Resources>
        <conv:DmsDataToInfo x:Key="DmsData2Info"/>
    </Window.Resources>
    
    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform x:Name="sctScale"/>
        </Grid.LayoutTransform>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="230"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0">DMS records</Label>
        <Label Grid.Row="0" Grid.Column="1">Description</Label>

        <ListView x:Name="lsvItems" Grid.Row="1" Grid.Column="0"
                  ItemsSource="{Binding Items}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="local:DmsSaveDialog.DmsData">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0"
                                  IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                                  Margin="2"
                                  Padding="4"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding Name, Mode=TwoWay}"
                                 Margin="2"
                                 Padding="4"
                                 IsEnabled="{Binding IsEnabled, Mode=OneWay}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Label Grid.Row="1" Grid.Column="1"
               Content="{Binding SelectedItem, ElementName=lsvItems, Converter={StaticResource DmsData2Info}}"
               Margin="4"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="Save"
                    Width="75"
                    Margin="4"
                    Click="Ok_Click"/>
            <Button Content="Cancel"
                    IsCancel="True"
                    Width="75"
                    Margin="4"/>
        </StackPanel>
    </Grid>
</Window>
